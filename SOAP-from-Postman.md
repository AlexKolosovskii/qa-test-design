# 🌍 SOAP API Testing: Country Information Service

## 📋 Описание задачи

**Цель:**  
Создать коллекцию в Postman для тестирования SOAP-сервиса, предоставляющего информацию о странах (Country Info Service).

**Тестируемые методы:**
1. ListOfContinentsByName — Список континентов по названию.
2. CountryName — Получение названия страны по ISO-коду.
3. FullCountryInfo — Полная информация о стране (флаг, валюта, язык и т.д.).
4. LanguageName — Получение названия языка по коду.

**Тестовые данные:**
- Страна: Россия (ISO код: RU).
- Негативные сценарии: Проверка обработки невалидных ISO-кодов, пустых значений и несуществующих языков.

---

## 🔗 Публичная коллекция Postman

Коллекция настроена, содержит как позитивные, так и негативные тест-кейсы для SOAP-методов.

🔗 **[Открыть коллекцию "Country Info Service (SOAP)" в Postman](https://alexkolosovskii-3552711.postman.co/workspace/GitBeast's-Workspace~7532925b-5d23-4da9-87d4-c083e0d14591/collection/52488976-e596f1c2-2183-4cb5-b590-07034e7f9afb?action=share&creator=52488976)**

**Структура коллекции:**
Country Info Service/
  ├── Позитивные тесты (Positive Cases)/
  │   ├── Get Country Name (RU) — Проверка получения названия России
  │   ├── Get Full Country Info (RU) — Проверка полной информации (флаг, валюта RUB, язык Russian)
  │   ├── List Continents by Name — Получение списка континентов
  │   └── Get Language Name (ru) — Проверка названия языка
  └── Негативные тесты (Negative Cases)/
      ├── Invalid ISO Code (XX) — Запрос несуществующего кода страны
      ├── Empty ISO Code — Запрос с пустым значением
      ├── Invalid Language Code — Запрос несуществующего языка
      └── Case Sensitivity Check — Проверка чувствительности к регистру (ru vs RU)

---

## 🧪 Сценарии тестирования

### Позитивные проверки (Happy Path)
| Метод | Входные данные | Ожидаемый результат |
|-------|----------------|---------------------|
| CountryName | sCountryISOCode: "RU" | Возвращает "Russia" (статус 200 OK) |
| FullCountryInfo | sCountryISOCode: "RU" | Возвращает объект с флагом, валютой (RUB), столицей (Moscow) |
| LanguageName | sISOCodeLanguage: "ru" | Возвращает "Russian" |
| ListOfContinentsByName | (без параметров или фильтр) | Возвращает список континентов |

### Негативные проверки (Negative Cases)
Добавлены в коллекцию для демонстрации глубины тестирования.

| Метод | Входные данные | Ожидаемый результат |
|-------|----------------|---------------------|
| CountryName | sCountryISOCode: "XX" (несуществующий код) | Возвращает пустое значение или специфичное сообщение об ошибке, статус 200 (SOAP стандарт) |
| CountryName | sCountryISOCode: "" (пустая строка) | Ошибка валидации или пустой ответ |
| FullCountryInfo | sCountryISOCode: "ZZZ" | Отсутствие данных в теле ответа |
| LanguageName | sISOCodeLanguage: "xyz" | Пустой результат |

Важно: Особенность SOAP в том, что он часто возвращает статус 200 OK даже при ошибках логики (например, страна не найдена). Ошибку нужно искать внутри XML-тела ответа (теги soap:Body или специфичные сообщения). Автотесты в коллекции проверяют именно содержание XML, а не только HTTP-статус.

---

## 🛠 Технические детали

**Формат запроса:** SOAP 1.1/1.2 (XML).
**Заголовки:** Content-Type: text/xml; charset=utf-8, SOAPAction.
**Автотесты:** В каждом запросе добавлены скрипты на JavaScript для парсинга XML-ответа и проверки наличия ожидаемых узлов (например, проверка, что торт <m:name> содержит "Russia").

---

## 🖥️ Тестовое окружение

| Компонент | Значение |
|-----------|----------|
| OS | Windows 11 |
| Browser | Chrome 145.0.7632.77 |
| Postman | Desktop App (последняя версия) |
| Environment | QA (переменные: baseUrl, countryISO: RU, langISO: ru) |

---
