# 🍃 MongoDB Practice: card_info Migration

## 📋 Описание задачи

**Цель:**  
Освоить работу с документоориентированной базой данных MongoDB. Создать кластер в MongoDB Atlas, подключиться через MongoDB Compass и перенести данные из реляционной таблицы card_info (MySQL) в NoSQL коллекцию.

**Инструменты:**
- MongoDB Atlas: Облачный кластер (Free Tier).
- MongoDB Compass: GUI клиент для управления базами данных.
- Источник данных: Таблица card_info из БД intern_shop (MySQL).

**Этапы выполнения:**
1. Регистрация в MongoDB Atlas и создание кластера.
2. Настройка доступа (Network Access, Database User).
3. Подключение через MongoDB Compass.
4. Экспорт данных из MySQL или ручной перенос документов в коллекцию card_info.
5. Выполнение практических запросов (CRUD операции) согласно заданию.

---

## 📥 Решение (Отчет о выполнении)

Все шаги, скриншоты подключения, структура документа и результаты выполнения запросов зафиксированы в Google Таблице.

🔗 **[Открыть таблицу с отчетом по MongoDB (Google Sheets)](https://docs.google.com/spreadsheets/d/1TbNIwaau_vW6znahRfQ__ptvKXpCqY3LXYBORt1jt4c/edit?usp=sharing)**

**Содержание отчета:**
| Шаг задания | Действие | Результат / Скриншот |
|-------------|----------|----------------------|
| 1 | Создание кластера в Atlas | Скриншот интерфейса Atlas с активным кластером |
| 2 | Настройка Network Access (0.0.0.0/0) | Скриншот настроек доступа |
| 3 | Подключение в MongoDB Compass | Скриншот успешного подключения (Connection String) |
| 4 | Создание БД и коллекции card_info | Скриншот структуры коллекции в Compass |
| 5 | Вставка документов (Insert Many) | Скриншот вставленных документов (JSON формат) |
| 6 | Выполнение запросов (Find, Update, Delete) | Скриншоты результатов выполнения команд в Compass |

Полный список выполненных действий и скриншоты доступны по ссылке выше.

---

## 🧪 Особенности перехода SQL → NoSQL

**Структура данных:**
В отличие от строгой табличной структуры MySQL, в MongoDB данные хранятся в формате BSON (бинарный JSON).

**Пример документа в коллекции card_info:**
{
  "_id": ObjectId("..."),
  "user_id": 123,
  "card_number": "4111111111111111",
  "card_holder": "ALEXANDER KOLOSOVSKII",
  "expiration_date": "12/28",
  "cvv": "123",
  "is_active": true,
  "created_at": ISODate("2026-02-23T10:00:00Z")
}

**Ключевые отличия, отраженные в задании:**
1. Отсутствие схемы: Поля могут отличаться в разных документах (хотя в данном случае структура единая).
2. _id: Уникальный идентификатор генерируется автоматически, если не указан явно.
3. Вложенность: При желании данные о владельце карты можно было бы вложить прямо в документ пользователя, но в рамках задачи создана отдельная коллекция, аналогичная таблице SQL.

---

## 🛠 Выполненные операции (CRUD)

В ходе работы были отработаны следующие команды MongoDB:

1. Create (Вставка):
   - Команда: db.card_info.insertOne({...}) или insertMany([...]).
   - Результат: Успешное добавление тестовых карт в коллекцию.

2. Read (Чтение):
   - Команда: db.card_info.find({ user_id: 123 }).
   - Фильтрация: Поиск активных карт, поиск по маске номера.

3. Update (Обновление):
   - Команда: db.card_info.updateOne({ _id: ... }, { $set: { is_active: false } }).
   - Результат: Деактивация устаревшей карты без удаления записи.

4. Delete (Удаление):
   - Команда: db.card_info.deleteOne({ card_number: "..." }).
   - Результат: Удаление тестовой записи.

---

## 🖥️ Тестовое окружение

| Компонент | Значение |
|-----------|----------|
| OS | Windows 11 |
| DB Client | MongoDB Compass (последняя версия) |
| DB Server | MongoDB Atlas Cluster (Free Tier M0) |
| Source DB | MySQL intern_shop (таблица card_info) |

---
