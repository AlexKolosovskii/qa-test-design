# 📦 Postman Collection: DemoShopping API

## 📋 Описание задачи

**Цель:**  
Настроить коллекцию запросов в Postman для тестирования API интернет-магазина DemoShopping, добавить автотесты и переменные окружения.

**Требования к выполнению:**
- Коллекция: Создать коллекцию "DemoShopping" с папками "Products" и "Cart".
- Методы: Оформить все endpoints из Swagger, относящиеся к товарам и корзине.
- PUT /products/{id}: Убрать id из тела запроса (остаётся только в URL).
- Авторизация: Настроить токен для запросов к корзине (метод в Users).
- Проверки: Основываться на статус-кодах + добавить автотесты в Postman.
- Именование: Названия запросов должны отражать суть проверки.
- Environment: Создать переменные QA: baseUrl, key (token), productId и др.
- Публичность: Коллекция должна быть публичной для просмотра ментором.

**Приложение:**  
Swagger API: https://intern.demoshopping.ru/  
Postman-коллекция: публичная ссылка ниже.

---

## 🔗 Публичная коллекция Postman

Коллекция настроена, протестирована и опубликована для просмотра.

🔗 **[Открыть коллекцию "DemoShopping" в Postman](https://alexkolosovskii-3552711.postman.co/workspace/GitBeast's-Workspace~7532925b-5d23-4da9-87d4-c083e0d14591/collection/52488976-f8f186a9-3268-4a2a-8229-8d52533c29a3?action=share&creator=52488976)**

**Структура коллекции:**
DemoShopping/
  ├── Products/
  │   ├── GET /products — Получить список всех товаров
  │   ├── GET /products/{id} — Получить товар по ID
  │   ├── POST /products — Создать новый товар
  │   ├── PUT /products/{id} — Обновить товар (id только в URL)
  │   └── DELETE /products/{id} — Удалить товар
  ├── Cart/
  │   ├── GET /cart — Получить содержимое корзины (требуется auth)
  │   ├── POST /cart — Добавить товар в корзину (требуется auth)
  │   ├── PUT /cart/{itemId} — Изменить количество в корзине (требуется auth)
  │   └── DELETE /cart/{itemId} — Удалить товар из корзины (требуется auth)
  └── Users/
      └── POST /login — Получить токен авторизации (key)

---

## ⚙️ Переменные окружения (QA Environment)

| Переменная | Значение | Описание |
|------------|----------|----------|
| baseUrl | https://intern.demoshopping.ru/api | Базовый URL API |
| key | {{token}} | Токен авторизации (именно key для публичности) |
| productId | 123 | ID тестового товара для повторного использования |
| itemName | Test Product | Название для создаваемых тестовых товаров |
| responseTime | 500 | Максимальное допустимое время ответа (мс) |

Важно: Переменная токена названа key, а не token — это требование Postman для сохранения публичного доступа к коллекции.

---

## 🧪 Автотесты в Postman

Для методов Products реализованы следующие типы проверок:

1. Статус-код: Корректность ответа сервера (пример: pm.response.to.have.status(200)).
2. Время ответа: Производительность endpoint (пример: проверка, что время меньше 500 мс).
3. Тип данных: Структура тела ответа (проверка, что id — число).
4. Наличие полей: Обязательные ключи в ответе (name, price, category).
5. Изменение данных: POST/PUT действительно создают/меняют товар (сравнение name в запросе и ответе).
6. Удаление: DELETE возвращает 204 и товар исчезает (GET после DELETE должен вернуть 404).

Пример логики теста (вкладка Tests в Postman):
- Проверка статус-кода: pm.test("Status code is 200", function () { pm.response.to.have.status(200); });
- Проверка времени ответа: pm.test("Response time is less than 500ms", function () { pm.expect(pm.response.responseTime).to.be.below(500); });
- Проверка структуры ответа: pm.test("Response has required fields", function () { const jsonData = pm.response.json(); pm.expect(jsonData).to.have.property('id'); pm.expect(jsonData).to.have.property('name'); });

---

## 🔐 Авторизация для Cart

Порядок действий:
1. Выполнить запрос POST /login с валидными учётными данными.
2. Сохранить полученный токен в переменную {{key}} через скрипт: pm.environment.set("key", pm.response.json().token);
3. Во всех запросах к Cart добавить заголовок: Authorization: Bearer {{key}}.

---

## 🗄️ Сброс базы данных

Важно: Перед началом и после завершения тестирования выполнить метод "Reset Database" в Swagger.

Зачем это нужно:
- Удаляет тестовые товары, созданные в ходе работы.
- Очищает корзину от тестовых заказов.
- Возвращает API в чистое состояние для других тестировщиков.

Скрипт также запускается автоматически ежедневно в 05:00 GMT+3.

---

## 🖥️ Тестовое окружение

| Компонент | Значение |
|-----------|----------|
| OS | Windows 11 |
| Browser | Chrome 145.0.7632.77 |
| Postman | Desktop App (последняя версия) |
| Environment | QA (переменные: baseUrl, key, productId) |

---

## 🗒️ Примечания и рекомендации

Ключевые моменты при работе с коллекцией:
1. Всегда проверяйте, что выбрано окружение QA перед запуском запросов.
2. Для запросов к Cart сначала выполните POST /login, чтобы получить {{key}}.
3. В методе PUT /products/{id} id передаётся только в URL, в теле запроса его быть не должно.
4. Автотесты запускаются автоматически после каждого запроса — следите за вкладкой "Test Results".

Если коллекция не отображается:
- Убедитесь, что ссылка открыта в браузере, где вы авторизованы в Postman.
- Проверьте, что коллекция имеет статус "Public" в настройках sharing.
- При необходимости — запросите доступ у владельца.
