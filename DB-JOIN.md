# 🗄️ SQL Practice Part 2: Orders & Payments

## 📋 Описание задачи

**Цель:**  
Закрепить навыки работы с реляционной базой данных `intern_shop`, выполнив сценарий создания и оплаты заказов, а также последующей аналитики данных с использованием псевдонимов таблиц (Aliases).

**Инструменты:** DBeaver / MySQL Workbench.
**База данных:** `intern_shop`

**Параметры подключения:**
| Параметр | Значение |
|----------|----------|
| **Hostname** | 176.119.147.174 |
| **Port** | 3306 |
| **Username** | group7 |
| **Password** | An0th3rC0mpl3xP@ss!2024 |
| **Schema** | intern_shop |

**Сценарий работы:**
1. Создание нескольких заказов для тестового пользователя (через GUI приложения или API).
2. Оплата заказов валидными тестовыми картами.
3. Выполнение SQL-запросов для проверки созданных данных.
4. Фиксация запросов и результатов в таблице отчета.

---

## 📥 Решение (SQL-запросы)

> Все выполненные запросы, включая создание заказов и итоговую аналитику, зафиксированы в Google Таблице. Особое внимание уделено использованию псевдонимов (AS) для улучшения читаемости кода.

🔗 **[Открыть таблицу с SQL-запросами (Google Sheets)](https://docs.google.com/spreadsheets/d/1HGuWI0x2J5p7RXWr_qL2n7WzsOfYtEDwxPf3uSgRyws/edit?usp=sharing)**

**Структура таблицы с решением:**
| № этапа | Описание действия | SQL-запрос (с псевдонимами) | Результат выполнения |
|---------|-------------------|-----------------------------|----------------------|
| 1 | Проверка созданных заказов | `SELECT o.id, o.status, u.username FROM orders AS o JOIN users AS u ON o.user_id = u.id WHERE u.username = '...'` | Список новых заказов со статусом "Оплачен" |
| 2 | Детализация состава заказа | `SELECT oi.product_id, p.name, oi.quantity FROM order_items AS oi JOIN products AS p ON oi.product_id = p.id WHERE oi.order_id = ...` | Список товаров в конкретном заказе |
| 3 | Подсчет суммы заказа | `SELECT SUM(oi.price * oi.quantity) AS total_sum FROM order_items AS oi WHERE oi.order_id = ...` | Итоговая сумма заказа |
| 4 | История платежей пользователя | `SELECT p.payment_date, p.amount, p.card_mask FROM payments AS p JOIN orders AS o ON p.order_id = o.id WHERE o.user_id = ...` | История успешных транзакций |
| 5 | Статистика по всем заказам пользователя | `SELECT COUNT(o.id) AS orders_count, SUM(o.total_amount) AS spent_total FROM orders AS o WHERE o.user_id = ...` | Количество заказов и общая потраченная сумма |

*(Полный список запросов и скриншоты результатов доступны по ссылке выше)*

---

## 🧪 Ключевые особенности решения

**Использование псевдонимов (Aliases):**
В каждом запросе использованы псевдонимы для таблиц (например, `orders AS o`, `users AS u`).
*Зачем это нужно:*
- Укорачивает код (не нужно писать `intern_shop.orders.user_id`, достаточно `o.user_id`).
- Повышает читаемость, особенно в сложных JOIN-конструкциях.
- Позволяет легко переименовывать колонки в выводе (например, `SUM(...) AS total_spent`).

**Проверка целостности данных:**
Запросы подтверждают, что:
- Заказы корректно связались с пользователем (`user_id`).
- Товары в заказах соответствуют справочнику `products`.
- Платежи прошли успешно и статусы заказов обновились.

---

## 🖥️ Тестовое окружение

| Компонент | Значение |
|-----------|----------|
| **OS** | Windows 11 |
| **DB Client** | DBeaver Community Edition |
| **DB Server** | MySQL (удаленный сервер курса) |
| **Тестовый пользователь** | (Ваш логин, под которым создавались заказы) |
| **Платежные данные** | Валидные тестовые карты из требований |

---
